query GetWorkExperiences {
  workExperiences(pagination: { limit: 100 }, sort: "startDate:desc") {
    documentId
    title
    company
    companyUrl
    startDate
    endDate
    description
    isRemote
    location
    logo {
      ...MediaFields
    }
    technologies {
      ...TechnologyFields
    }
  }
}
