fragment MediaFields on UploadFile {
  documentId
  url
  alternativeText
  width
  height
  formats
}

fragment TechnologyFields on Technology {
  documentId
  name
  slug
  category
  websiteUrl
  icon {
    ...MediaFields
  }
}

fragment TagFields on Tag {
  documentId
  name
  slug
  color
  category
}
