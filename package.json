{
  "name": "paulislava-space",
  "private": true,
  "workspaces": [
    "packages/cms",
    "packages/web"
  ],
  "scripts": {
    "dev:cms": "npm run dev --workspace=packages/cms",
    "dev:web": "npm run dev --workspace=packages/web --env-file=.env",
    "build": "npm run build --workspace=packages/cms && npm run build --workspace=packages/web"
  },
  "engines": {
    "node": ">=20"
  }
}
